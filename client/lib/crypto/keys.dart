// Identity keypair (X25519) + signing keypair (Ed25519) generation on signup.
// PRIVATE keys -> flutter_secure_storage (Keychain/Keystore), hardware-backed
// where available. Only PUBLIC keys uploaded. (spec 2.3 / 6)
