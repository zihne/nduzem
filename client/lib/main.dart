// OpaqueShare Flutter client.
// Crypto is client-side (libsodium). Private keys never leave the device.
// Open-source under Apache-2.0 (spec 14) to enable verifiable builds + audit.
void main() {
  // runApp(OpaqueShareApp());
}
